services:
  web:
    build: .
    container_name: kiralama_web
    restart: unless-stopped
    command: gunicorn --reload --worker-class gthread --workers 1 --threads 4 --timeout 90 --graceful-timeout 30 --keep-alive 5 --bind 0.0.0.0:10000 run:app
    ports:
      - "5000:10000"
    volumes:
      - .:/app
    environment:
      - DATABASE_URL=postgresql://postgres:gizlisifre@db:5432/kiralama_db
      - TEMPLATES_AUTO_RELOAD=true
      - DISABLE_STATIC_CACHE=true
    depends_on:
      db:
        condition: service_healthy

  db:
    image: postgres:15-alpine
    container_name: kiralama_veritabani
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: gizlisifre
      POSTGRES_DB: kiralama_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d kiralama_db"]
      interval: 3s
      timeout: 3s
      retries: 10
      start_period: 5s

volumes:
  postgres_data: